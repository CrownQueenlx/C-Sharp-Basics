// Declating a variable to use in out loop's boolean expression
int number = 0;

// While the value in number is less than or equal to 100, run the following
while (number <= 100)
{
    // Write the value of number to the console and then increament by 5
    Console.WriteLine(number);
    number += 5;
}